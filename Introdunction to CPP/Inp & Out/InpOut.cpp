#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    int a, b;
    cin >> a >> b; // this is the input method of c using right shift(>>)

    cout << "The value of a and b is : " << a << " " << b << endl; // this is the output method using left shit(<<)
    return 0;
}

// here if we take input in nextline then it'll execute and if we take input
// as space separated than it will also execute ----> In both cases it will execute